<Window x:Class="PL.Admin.VolunteerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Volunteer Details" Height="700" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="#F8F9FA"
        Loaded="Window_Loaded"
        Closed="Window_Closed"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">

    <Window.Resources>
        <Style x:Key="ModernCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="0" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Label">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="5,5,0,10"/>
            <Setter Property="BorderBrush" Value="#D1D1D1"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost"
                                  VerticalAlignment="Center"
                                  HorizontalScrollBarVisibility="Hidden"
                                  VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style TargetType="ComboBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Margin" Value="5,5,0,10"/>
            <Setter Property="BorderBrush" Value="#D1D1D1"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#667eea"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="15,20,15,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Border Style="{StaticResource ModernCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Text="Volunteer Profile" FontSize="24" FontWeight="Bold" Foreground="#34495E" Margin="0,0,0,20" Grid.Row="0" HorizontalAlignment="Center"/>

                <StackPanel Grid.Row="1">
                    <Label Content="ID"/>
                    <TextBox Text="{Binding CurrentVolunteer.Id}" IsReadOnly="True"/>

                    <Label Content="Name"/>
                    <TextBox Text="{Binding CurrentVolunteer.Name}"/>

                    <Label Content="Phone"/>
                    <TextBox Text="{Binding CurrentVolunteer.Phone}"/>

                    <Label Content="Email"/>
                    <TextBox Text="{Binding CurrentVolunteer.Email}"/>

                    <Label Content="Password"/>
                    <TextBox Text="{Binding CurrentVolunteer.Password, Mode=OneWayToSource}"/>

                    <Label Content="Address"/>
                    <TextBox Text="{Binding CurrentVolunteer.Address}"/>

                    <Label Content="Job"/>
                    <ComboBox ItemsSource="{Binding Source={StaticResource JobsCollectionKey}}"
                              SelectedValue="{Binding CurrentVolunteer.Jobs}"/>

                    <Label Content="Is Active"/>
                    <CheckBox IsChecked="{Binding CurrentVolunteer.IsActive}" Margin="5"/>

                    <Label Content="Max Distance"/>
                    <TextBox Text="{Binding CurrentVolunteer.MaxDistance}"/>

                    <Label Content="Distance Type"/>
                    <ComboBox ItemsSource="{Binding Source={StaticResource DistanceCollectionKey}}"
                              SelectedValue="{Binding CurrentVolunteer.DistanceType}"/>

                    <Label Content="Sum of Calls"/>
                    <TextBox Text="{Binding CurrentVolunteer.SumOfCalls}" IsReadOnly="True"/>

                    <Label Content="Cancellations"/>
                    <TextBox Text="{Binding CurrentVolunteer.SumOfCancellation}" IsReadOnly="True"/>

                    <Label Content="Expired Calls"/>
                    <TextBox Text="{Binding CurrentVolunteer.SumOfExpiredCalls}" IsReadOnly="True"/>

                    <Label Content="Current Call"/>
                    <TextBox Text="{Binding CurrentVolunteer.CallInProgress, Converter={StaticResource CallInProgressConverter}}" IsReadOnly="True"/>
                </StackPanel>

                <Button Grid.Row="2" Content="{Binding ButtonText}" Click="btnAddUpdate_Click" HorizontalAlignment="Center"/>
            </Grid>
        </Border>
    </ScrollViewer>
</Window>
