<Window x:Class="PL.Admin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
        Title="Main Management Screen" Height="700" Width="1200" WindowStartupLocation="CenterScreen"
        Loaded="MainWindow_Loaded"
        Closed="MainWindow_Closed"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <!-- Modern Card Style -->
        <Style x:Key="ModernCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Padding" Value="25"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#E8EAF0" BlurRadius="20" ShadowDepth="0" Opacity="0.8"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Primary Button Style -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#667eea" Offset="0"/>
                        <GradientStop Color="#764ba2" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="#667eea" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#764ba2" Offset="0"/>
                                            <GradientStop Color="#667eea" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Time Control Button Style -->
        <Style x:Key="TimeButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E3F2FD"/>
                                <Setter Property="BorderBrush" Value="#2196F3"/>
                                <Setter Property="Foreground" Value="#1976D2"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#BBDEFB"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#F5F5F5"/>
                                <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                <Setter Property="Foreground" Value="#9E9E9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#E9ECEF"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                        VerticalAlignment="Center"
                                        HorizontalScrollBarVisibility="Hidden"
                                        VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#667eea"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#F5F5F5"/>
                                <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                <Setter Property="Foreground" Value="#9E9E9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Section Title Style -->
        <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>
    </Window.Resources>

    <!-- Main Background -->
    <Border CornerRadius="15">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#F8F9FA" Offset="0"/>
                <GradientStop Color="#E9ECEF" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect Color="#ADB5BD" BlurRadius="25" ShadowDepth="0" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <!-- Header -->
            <Border Height="80" VerticalAlignment="Top" CornerRadius="15,15,0,0">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#667eea" Offset="0"/>
                        <GradientStop Color="#764ba2" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="Management Dashboard" 
                              FontSize="24" 
                              FontWeight="Bold" 
                              Foreground="White" 
                              HorizontalAlignment="Center"/>
                    <TextBlock Text="System Control Center" 
                              FontSize="12" 
                              Foreground="#E8E8E8" 
                              HorizontalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- Main Content -->
            <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,80,0,0">
                <Grid Margin="30">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- System Clock Card -->
                    <Border Grid.Row="0" Style="{StaticResource ModernCardStyle}">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="System Time" Style="{StaticResource SectionTitleStyle}" HorizontalAlignment="Center"/>
                            <TextBlock Text="{Binding CurrentTime}" 
                                      FontSize="42" 
                                      FontWeight="Bold" 
                                      Foreground="#667eea" 
                                      HorizontalAlignment="Center" 
                                      Margin="0,10,0,20"/>
                        </StackPanel>
                    </Border>

                    <!-- Time Controls Card -->
                    <Border Grid.Row="1" Style="{StaticResource ModernCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Time Controls" Style="{StaticResource SectionTitleStyle}"/>
                            <UniformGrid Columns="5" Margin="0,10">
                                <Button Content="+1 Minute" 
                                       Click="btnAddOneMinute_Click" 
                                       Width="140" Height="45" 
                                       Margin="5"
                                       Style="{StaticResource TimeButtonStyle}"
                                       IsEnabled="{Binding IsSimulatorRunning, Converter={StaticResource InverseBoolConverter}}"/>
                                <Button Content="+1 Hour" 
                                       Click="btnAddOneHour_Click" 
                                       Width="140" Height="45" 
                                       Margin="5"
                                       Style="{StaticResource TimeButtonStyle}"
                                       IsEnabled="{Binding IsSimulatorRunning, Converter={StaticResource InverseBoolConverter}}"/>
                                <Button Content="+1 Day" 
                                       Click="btnAddOneDay_Click" 
                                       Width="140" Height="45" 
                                       Margin="5"
                                       Style="{StaticResource TimeButtonStyle}"
                                       IsEnabled="{Binding IsSimulatorRunning, Converter={StaticResource InverseBoolConverter}}"/>
                                <Button Content="+1 Month" 
                                       Click="btnAddOneMonth_Click" 
                                       Width="140" Height="45" 
                                       Margin="5"
                                       Style="{StaticResource TimeButtonStyle}"
                                       IsEnabled="{Binding IsSimulatorRunning, Converter={StaticResource InverseBoolConverter}}"/>
                                <Button Content="+1 Year" 
                                       Click="btnAddOneYear_Click" 
                                       Width="140" Height="45" 
                                       Margin="5"
                                       Style="{StaticResource TimeButtonStyle}"
                                       IsEnabled="{Binding IsSimulatorRunning, Converter={StaticResource InverseBoolConverter}}"/>
                            </UniformGrid>
                        </StackPanel>
                    </Border>

                    <!-- Risk Range & Simulator Card -->
                    <Border Grid.Row="2" Style="{StaticResource ModernCardStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Risk Range Section -->
                            <StackPanel Grid.Column="0" Margin="0,0,20,0">
                                <TextBlock Text="Risk Range Configuration" Style="{StaticResource SectionTitleStyle}"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,10">
                                    <TextBox Width="180" Height="40"
                                            Text="{Binding RiskRange, UpdateSourceTrigger=PropertyChanged}"
                                            Style="{StaticResource ModernTextBoxStyle}"
                                            Margin="0,0,15,0"
                                            IsEnabled="{Binding IsSimulatorRunning, Converter={StaticResource InverseBoolConverter}}"/>
                                    <Button Content="Update Range"
                                           Click="UpdateRiskRange_Click"
                                           Width="140" Height="40"
                                           Style="{StaticResource PrimaryButtonStyle}"
                                           IsEnabled="{Binding IsSimulatorRunning, Converter={StaticResource InverseBoolConverter}}"/>
                                </StackPanel>
                            </StackPanel>

                            <!-- Simulator Section -->
                            <StackPanel Grid.Column="1" Margin="20,0,0,0">
                                <TextBlock Text="Simulator Control" Style="{StaticResource SectionTitleStyle}"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,10">
                                    <TextBox Width="100" Height="40" 
                                            Text="{Binding Interval, UpdateSourceTrigger=PropertyChanged}" 
                                            Style="{StaticResource ModernTextBoxStyle}"
                                            Margin="0,0,15,0"
                                            IsEnabled="{Binding IsSimulatorRunning, Converter={StaticResource InverseBoolConverter}}"/>
                                    <Button Content="{Binding IsSimulatorRunning, Converter={StaticResource SimulatorButtonTextConverter}}"
                                           Click="ToggleSimulator_Click"
                                           Width="140" Height="40"
                                           Style="{StaticResource PrimaryButtonStyle}"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Management Actions Card -->
                    <Border Grid.Row="3" Style="{StaticResource ModernCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Management Actions" Style="{StaticResource SectionTitleStyle}"/>
                            <UniformGrid Columns="2" Rows="2" Margin="0,15">
                                <Button Content="Handle Volunteer" 
                                       Click="HandleVolunteer_Click" 
                                       Width="200" Height="50" 
                                       Margin="10"
                                       Style="{StaticResource PrimaryButtonStyle}"/>
                                <Button Content="Handle Calls" 
                                       Click="HandleCall_Click" 
                                       Width="200" Height="50" 
                                       Margin="10"
                                       Style="{StaticResource PrimaryButtonStyle}"/>
                                <Button Content="Initialize Database" 
                                       Click="InitializeDB_Click" 
                                       Width="200" Height="50" 
                                       Margin="10"
                                       Style="{StaticResource PrimaryButtonStyle}"
                                       IsEnabled="{Binding IsSimulatorRunning, Converter={StaticResource InverseBoolConverter}}"/>
                                <Button Content="Reset Database" 
                                       Click="ResetDB_Click" 
                                       Width="200" Height="50" 
                                       Margin="10"
                                       Style="{StaticResource PrimaryButtonStyle}"
                                       IsEnabled="{Binding IsSimulatorRunning, Converter={StaticResource InverseBoolConverter}}"/>
                            </UniformGrid>
                        </StackPanel>
                    </Border>
                </Grid>
            </ScrollViewer>

            <!-- Close Button -->
            <Button Content="✕"
                    Width="35" Height="35"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="0,15,15,0"
                    Background="Transparent"
                    BorderThickness="0"
                    Foreground="White"
                    FontSize="18"
                    FontWeight="Bold"
                    Cursor="Hand"
                    Click="MainWindow_Closed">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="17">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#E74C3C"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window>