<Window x:Class="PL.Volunteer.VolunteerMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Volunteer Home"
        Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#FAFAFA">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <!-- Volunteer Info Panel -->
        <StackPanel Grid.Column="0" Background="#F0F4F8" Margin="0,0,10,0">
            <TextBlock Text="Volunteer Profile" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="Name:" FontWeight="Bold"/>
            <TextBlock Text="{Binding CurrentVolunteer.Name}" Margin="0,0,0,10"/>

            <TextBlock Text="Job:" FontWeight="Bold"/>
            <TextBlock Text="{Binding CurrentVolunteer.Jobs}" Margin="0,0,0,10"/>

            <TextBlock Text="Address:" FontWeight="Bold"/>
            <TextBlock Text="{Binding CurrentVolunteer.Address}" Margin="0,0,0,10"/>

            <TextBlock Text="Phone:" FontWeight="Bold"/>
            <TextBlock Text="{Binding CurrentVolunteer.Phone}" Margin="0,0,0,10"/>

            <TextBlock Text="Email:" FontWeight="Bold"/>
            <TextBlock Text="{Binding CurrentVolunteer.Email}" Margin="0,0,0,10"/>
        </StackPanel>

        <!-- Action Buttons Panel -->
        <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Width="250">
            <Button Content="Update My Details"
                    Click="UpdateVolunteer_Click"
                    Height="40" FontWeight="Bold" Background="#007ACC" Foreground="White" BorderThickness="0"/>

            <Button Content="View Closed Calls"
                    Click="ViewClosedCalls_Click"
                    Height="40" FontWeight="Bold" Background="#4CAF50" Foreground="White" BorderThickness="0"/>

            <Button Content="View Open Calls"
                    Click="ViewOpenCalls_Click"
                    Height="40" FontWeight="Bold" Background="#FF9800" Foreground="White" BorderThickness="0"/>

            <Button Content="Exit"
                    Click="Exit_Click"
                    Height="40" FontWeight="Bold" Background="#E53935" Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
