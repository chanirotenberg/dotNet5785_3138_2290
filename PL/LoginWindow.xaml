<Window x:Class="PL.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="530" Width="500"
        WindowStartupLocation="CenterScreen"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <!-- Modern Button Style -->
        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#667eea" Offset="0"/>
                        <GradientStop Color="#764ba2" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="25,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="25"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="#4A90E2" BlurRadius="15" ShadowDepth="2" Opacity="0.3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#764ba2" Offset="0"/>
                                            <GradientStop Color="#667eea" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="12"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="#E0E6ED" BlurRadius="10" ShadowDepth="0" Opacity="0.8"/>
                            </Border.Effect>
                            <ScrollViewer x:Name="PART_ContentHost"
                                        VerticalAlignment="Center"
                                        HorizontalScrollBarVisibility="Hidden"
                                        VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="Background" Value="White"/>
                                <Setter TargetName="PART_ContentHost" Property="Margin" Value="0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Label Style -->
        <Style x:Key="ModernLabelStyle" TargetType="Label">
            <Setter Property="Foreground" Value="#34495E"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Margin" Value="5,0,0,5"/>
        </Style>

        <!-- Modern CheckBox Style -->
        <Style x:Key="ModernCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#7F8C8D"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="CheckBoxBorder"
                                    Width="18" Height="18"
                                    CornerRadius="4"
                                    Background="White"
                                    BorderBrush="#BDC3C7"
                                    BorderThickness="2"
                                    Margin="0,0,8,0">
                                <Path x:Name="CheckMark"
                                      Data="M 0 3 L 3 6 L 8 1"
                                      Stroke="#667eea"
                                      StrokeThickness="2.5"
                                      Visibility="Collapsed"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"/>
                            </Border>
                            <ContentPresenter VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="CheckBoxBorder" Property="Background" Value="#F8F9FA"/>
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="#667eea"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBoxBorder" Property="BorderBrush" Value="#667eea"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Main Container with Glass Effect -->
    <Border CornerRadius="20" Background="White" Opacity="0.95">
        <Border.Effect>
            <DropShadowEffect Color="#34495E" BlurRadius="30" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>

        <Grid>
            <!-- Background Gradient -->
            <Border CornerRadius="20">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1" Opacity="0.03">
                        <GradientStop Color="#667eea" Offset="0"/>
                        <GradientStop Color="#764ba2" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
            </Border>

            <!-- Header Section -->
            <StackPanel VerticalAlignment="Top" Margin="0,30,0,0">
                <TextBlock Text="Welcome Back"
                          FontSize="28"
                          FontWeight="Light"
                          Foreground="#2C3E50"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,5"/>
                <TextBlock Text="Sign in to your account"
                          FontSize="14"
                          Foreground="#7F8C8D"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,40"/>
            </StackPanel>

            <!-- Main Content -->
            <Grid Margin="60,120,60,30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Volunteer ID -->
                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <Label Content="Volunteer ID:" Style="{StaticResource ModernLabelStyle}"/>
                    <TextBox Text="{Binding VolunteerId, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource ModernTextBoxStyle}"/>
                </StackPanel>

                <!-- Password -->
                <StackPanel Grid.Row="1" Margin="0,0,0,15">
                    <Label Content="Password:" Style="{StaticResource ModernLabelStyle}"/>
                    <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" 
                             Name="PasswordTextBox"
                             FontFamily="Consolas"
                             FontSize="14"
                             VerticalContentAlignment="Center">
                        <TextBox.Style>
                            <Style TargetType="TextBox" BasedOn="{StaticResource ModernTextBoxStyle}">
                                <Setter Property="Text" Value="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsPasswordHidden}" Value="True">
                                        <Setter Property="FontFamily" Value="Segoe UI Symbol"/>
                                        <Setter Property="Foreground" Value="Transparent"/>
                                        <Setter Property="CaretBrush" Value="Black"/>
                                        <Setter Property="Background" Value="White"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </StackPanel>

                <!-- Show/Hide Password -->
                <CheckBox Grid.Row="2" Content="Show password" 
                          IsChecked="{Binding IsPasswordHidden, Converter={StaticResource InverseBoolConverter}}"
                          Margin="5,0,0,25"
                          Style="{StaticResource ModernCheckBoxStyle}"/>

                <!-- Login Button -->
                <Button Grid.Row="3" Content="Login" Click="Login_Click"
                        Width="120" Height="45" 
                        HorizontalAlignment="Center" 
                        Margin="0,10"
                        Style="{StaticResource ModernButtonStyle}"/>

                <!-- Login Button -->
                <Button Grid.Row="3" Content="Login" Click="Login_Click"
                        Width="120" Height="45" 
                        HorizontalAlignment="Center" 
                        Margin="0,10"
                        Style="{StaticResource ModernButtonStyle}"/>
            </Grid>

            <!-- Close Button -->
            <Button Content="✕"
                    Width="30" Height="30"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Margin="0,15,15,0"
                    Background="Transparent"
                    BorderThickness="0"
                    Foreground="#BDC3C7"
                    FontSize="16"
                    Cursor="Hand"
                    Click="CloseButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="15">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#ECF0F1"/>
                                            <Setter Property="Foreground" Value="#E74C3C"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Border>
</Window>